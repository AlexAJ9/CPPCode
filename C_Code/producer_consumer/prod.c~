#include <stdio.h>
#include "shared_lib/shared.h"

int main(){
    sem_t sem1;
    sem1=sem_init(6255);
    sem_set(sem1,0);
	char *c;
	c=(char *)getmem(60205);
	for(char i='A';i<='Z';++i){
	       VS(sem1);
	       *c=i;
	       PS(sem1);
	}
	return 0; 
}
